local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "CARRY A FRIEND | BY MR.DACUDAO", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"})

--[[
Name = <CARRY A FRIEND> - The name of the UI.
HidePremium = <bool> - Whether or not the user details shows Premium status or not.
SaveConfig = <bool> - Toggles the config saving in the UI.
ConfigFolder = <string> - The name of the folder where the configs are saved.
IntroEnabled = <bool> - Whether or not to show the intro animation.
IntroText = <WOW YOU EXECUTE> - Text to show in the intro animation.
IntroIcon = <string> - URL to the image you want to use in the intro animation.
Icon = <string> - URL to the image you want displayed on the window.
CloseCallback = <function> - Function to execute when the window is closed.
]]

local Tab = Window:MakeTab({
	Name = "mine",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Section = Tab:AddSection({
	Name = "speed"
})
Tab:AddSlider({
	Name = "Slider",
	Min = 0,
	Max = 200,
	Default = 5,
	Color = Color3.fromRGB(255,255,255),
	Increment = 16,
	ValueName = "speed",
	Callback = function(Value)
		print(Value)
	end   
 })

Tab:AddButton({
	Name = "speed!",
	Callback = function()
      		print("speed pressed")
  	end    
})

local Tab = Window:MakeTab({
	Name = "about me",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Section = Tab:AddSection({
	Name = "info"
})

local Section = Tab:AddSection({
	Name = "hello the creator of the script is MR.DACUDAO HE CREATED THIS 
        SCRIPTfor EZ WAY i hope you good day 
        subs to MR.DACUDAO"
})
